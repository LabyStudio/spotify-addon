plugins {
    id 'java'
}

group 'de.labystudio'
version '1.3.2'

compileJava {
    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.github.LabyStudio:desktopmodules:2.5.2'
    implementation 'com.github.LabyStudio:desktopmodules:2.5.2:sources'
    //implementation project(':desktopmodules-core')

    implementation 'com.google.code.gson:gson:2.8.7'
}

allprojects {
    repositories {
        maven { url 'https://labystudio.de/maven/' }
    }
}